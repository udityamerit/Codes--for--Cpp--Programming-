/*  

Classes = Basic template for creating object

Object = Basic run time entities. 

Data Abstraction & Encapsulation = Warpping data and         function into single unit.

Inheritance = Peoperties of one class can be inherted into  others.

Polymorphism = abilty to take more than one forms.

Dynamic Binding = code which will excute is not known until the program.runs.

Message Passing = Object.message(Information) call format.

*/

